import React from 'react';
import SideBar from "../../components/sideBar";

const LocationsPage = () => {
    return (
        <div>
            <SideBar/>
        </div>
    );
};

export default LocationsPage;